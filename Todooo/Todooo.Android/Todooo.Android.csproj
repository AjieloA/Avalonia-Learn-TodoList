<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0-android</TargetFramework>
        <SupportedOSPlatformVersion>23</SupportedOSPlatformVersion>
        <Nullable>enable</Nullable>
        <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
        <ApplicationId>com.mds.dogshop</ApplicationId>
        <ApplicationVersion>1</ApplicationVersion>
        <ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
        <AndroidPackageFormat>apk</AndroidPackageFormat>
        <AndroidEnableProfiledAot>false</AndroidEnableProfiledAot>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)' == 'Release'">
        <!-- Release 默认输出单个 APK，便于分发给测试设备和第三方渠道。 -->
        <AndroidPackageFormat>apk</AndroidPackageFormat>
        <AndroidCreatePackagePerAbi>false</AndroidCreatePackagePerAbi>
        <DebugSymbols>false</DebugSymbols>
        <DebugType>none</DebugType>
        <!-- keystore 放在仓库根目录，本项目文件位于 Todooo/Todooo.Android，所以相对路径需要向上两级。 -->
        <DOGSHOP_ANDROID_KEYSTORE Condition="'$(DOGSHOP_ANDROID_KEYSTORE)' == ''">..\..\mds_dog_unity.keystore</DOGSHOP_ANDROID_KEYSTORE>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)' == 'Release' and Exists('$(DOGSHOP_ANDROID_KEYSTORE)')">
        <!-- 生产环境建议通过 CI/环境变量注入签名密码，避免把正式密钥信息提交到仓库。 -->
        <AndroidKeyStore>true</AndroidKeyStore>
        <AndroidSigningKeyStore>$(DOGSHOP_ANDROID_KEYSTORE)</AndroidSigningKeyStore>
        <AndroidSigningKeyAlias>dog</AndroidSigningKeyAlias>
        <AndroidSigningStorePass>mds@0626</AndroidSigningStorePass>
        <AndroidSigningKeyPass>mds@0626</AndroidSigningKeyPass>
    </PropertyGroup>

    <ItemGroup>
        <AndroidResource Include="Icon.png">
            <Link>Resources\drawable\Icon.png</Link>
        </AndroidResource>
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Avalonia.Android" Version="12.0.3"/>
        <PackageReference Include="Xamarin.AndroidX.Core.SplashScreen" Version="1.0.1.15"/>
        <!-- .NET Android 的 AndroidMavenLibrary 不解析 Gradle 的动态版本 "+"，这里固定到 Maven Central 当前可下载版本。 -->
        <AndroidMavenLibrary Include="com.tencent.mm.opensdk:wechat-sdk-android" Version="6.8.34" Bind="true" />
        <!-- 微信 AAR 中部分 Java 成员会生成重复 C# 名称，Metadata.xml 用于修正绑定命名冲突。 -->
        <TransformFile Include="Transforms\Metadata.xml" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Todooo\Todooo.csproj"/>
    </ItemGroup>
</Project>
